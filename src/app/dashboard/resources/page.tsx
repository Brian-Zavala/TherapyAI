// Performance-optimized version with CSS-only animations
// Replaced framer-motion with GPU-accelerated CSS transforms
// Implements content-visibility, CSS containment, and single IntersectionObserver pattern
// Uses @layer for cascade control and View Transitions API
export { default } from "./page-optimized";